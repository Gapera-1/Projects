-- SQL script to create the medicine_db database
-- Run this in MySQL command line or MySQL Workbench

CREATE DATABASE IF NOT EXISTS medicine_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- If you need to create a user (optional, you can use root):
-- CREATE USER IF NOT EXISTS 'medicine_user'@'localhost' IDENTIFIED BY 'your_password';
-- GRANT ALL PRIVILEGES ON medicine_db.* TO 'medicine_user'@'localhost';
-- FLUSH PRIVILEGES;

